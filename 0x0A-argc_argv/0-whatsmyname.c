#include <stdio.h>

/**
 * main - prints program's name
 *
 * @argc: int
 * @argv: list
 *
 * Return: Always 0 (Success)
 */
int main(int argc __attribute((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
