#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: using if loop to find if n is greater or less or equal to 0.
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int n;

	if (n < 0)
	{
		printf("is negative\n");
	} else
	{
		printf("is positive\n");
	} else
	{
		printf("is zero\n");
	}
	return (0);
}
